# 🎨 CREATIVE: Multiple Report Formats

🎨🎨🎨 ENTERING CREATIVE PHASE: Architecture

## Component Description
Подсистема формирования отчетов о тестировании YAxUnit. Отвечает за чтение параметров запуска, выбор генератора(ов) отчета по формату и запись файлов отчета(ов) по путям в файловой системе. Интеграция с jUnit (для CI/CD) и Allure (для анализа), а также поддержание обратной совместимости с существующими конфигами.

## Requirements & Constraints
- Формировать несколько отчетов за один прогон тестов
- Поддержать jUnit и Allure (существующие форматы)
- Обеспечить обратную совместимость с `reportFormat` и `reportPath`
- Предоставить возможность задавать путь отчета на уровне конкретного элемента (перекрывая общий `reportPath`)
- Минимизировать изменения в модулях конкретных форматов (jUnit/Allure)
- Сохранить текущее поведение `showReport` (UI не обязан отображать множественные пути; достаточно логировать все сформированные файлы)
- Надежная обработка ошибок: неизвестный формат не должен срывать формирование остальных отчетов

## Options

### Option A: Расширить `reportFormat` до массива/строки со списком
- Описание: Сохранить единственное поле `reportFormat`, разрешив передавать массив или строку со списком форматов (напр. `"junit,allure"`). Генерация по циклу.
- Pros:
  - Минимум изменений интерфейса
  - Простая миграция конфигов (старые значения валидны)
- Cons:
  - Размытый контракт поля: строка/массив, парсинг списка в одном поле
  - Нет естественного места для индивидуального `path` на элемент
  - Плохая расширяемость, сложнее документировать схему

### Option B: Новое поле `reports: Массив` с элементами `{ format, path? }`
- Описание: Добавить новое свойство `reports` в параметры запуска. Каждый элемент описывает формат и, опционально, свой путь. При отсутствии `reports` — автогенерация одного элемента из `reportFormat`/`reportPath` (обратная совместимость).
- Pros:
  - Четкий и расширяемый контракт (JSON-схема понятна)
  - Естественная поддержка индивидуальных путей
  - Простая бэкомпат-логика через автозаполнение массива
  - Низкая стоимость изменения текущей реализации
- Cons:
  - Потребуется адаптация чтения параметров и генерации

### Option C: Событийная/подписочная модель «репортеров»
- Описание: Ввести шину событий и подписчиков-генераторов, конфигурируемых списком. Каждому подписчику передаются параметры.
- Pros:
  - Наиболее гибкая и масштабируемая архитектура
  - Легко добавлять новые форматы без смены контрактов
- Cons:
  - Существенный рефакторинг, повышенная сложность
  - Выше риск регрессий и трудозатраты

### Option D: Сервис `ReportManager` (компоновщик стратегий)
- Описание: Ввести объект-компоновщик, содержащий список стратегий (форматов), управляет путями и логированием.
- Pros:
  - Лучшее разделение ответственности
  - Улучшаем тестопригодность
- Cons:
  - Выше трудоемкость, частичный рефакторинг контрактов

## Options Analysis
- A — быстрый путь, но плохо масштабируется, неудобен для отдельных путей, усложняет документацию
- B — наилучший баланс: строгий контракт, естественная поддержка путей, простая бэкомпат-история
- C — избыточно для текущего объема изменений
- D — полезно, но не обязательно на данном этапе; можно вернуться в будущем при росте числа форматов

## Recommended Approach
Рекомендация: Option B — ввести `reports: Массив` и автозаполнение из `reportFormat`/`reportPath` при отсутствии `reports`.
Обоснование: соответствует требованиям, сохраняет обратную совместимость, минимальны изменения в существующих генераторах, понятная документация и тестирование.

## Implementation Guidelines
- Изменить `ЮТФабрика.ПараметрыЗапуска()` — по умолчанию включать `reports = Новый Массив`.
- `ЮТПараметрыЗапускаСлужебный`:
  - При чтении JSON распознавать `reports` как массив структур `{format, path?}`
  - Если `reports` отсутствует или пуст, но заданы `reportFormat`/`reportPath` — сформировать `reports` из них
- `ЮТОтчетСлужебный`:
  - Если `reports` заполнен — выполнять цикл по элементам и формировать файл для каждого
  - Если `reports` пуст — использовать текущую совместимую логику (один отчет по `reportFormat`)
  - Для каждого элемента определять модуль-генератор по `format`, путь — `element.path` или общий `reportPath`
  - Логировать успешные пути и ошибки по каждому элементу, не прекращая общий процесс
- Не изменять интерфейсы конкретных форматов (`ЮТОтчетJUnitСлужебный`, `ЮТОтчетAllureСлужебный`)
- Документация: обновить `documentation/docs/features/reports/*` с примерами JSON

### Псевдокод формирования
```
Если Параметры.reports НЕ Пусто Тогда
	Для Каждого Элемент Из Параметры.reports Цикл
		Путь = Элемент.path ? Элемент.path : Параметры.reportPath;
		Формат = СтрЗначение(Элемент.format);
		Попытка
			Ген = НайтиГенераторПоФормату(Формат);
			СформироватьОтчетВФормате(Ген, Путь, КонтекстТестов);
			Лог("Report generated", Формат, Путь);
		Исключение
			ЛогОшибка("Report failed", Формат, Путь, ОписаниеОшибки());
		КонецПопытки;
	КонецЦикла;
Иначе
	// назад совместимый путь
	СформироватьОдиночныйОтчет(Параметры.reportFormat, Параметры.reportPath);
КонецЕсли;
```

## Verification Checkpoint
- Несколько отчетов формируются за один прогон (jUnit + Allure)
- Обратная совместимость: при старых полях формируется один отчет
- Индивидуальные пути работают; общий путь — как дефолт
- Неизвестный формат не прерывает процесс, остальные отчеты формируются
- Логи содержат пути всех сформированных отчетов
- Тесты покрывают основные и негативные сценарии

🎨🎨🎨 EXITING CREATIVE PHASE
